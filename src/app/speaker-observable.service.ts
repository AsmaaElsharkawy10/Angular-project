import { Injectable } from '@angular/core';
import { observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class SpeakerObservableService {

  constructor(speakerObservable:SpeakerObservableService) { }
}
